# 2026-07-17 工作日志

## v4 尝试：Linear 紫蓝深色（被否）
- 用户要求"科技感 + 专业"，原"产品经理创业"调性升级为"科技达人/科技分享"
- 切到 v4 紫蓝 #5E5CE6 + 光晕 + 脉动点 + mac 代码圆点
- 被否："这颜色搭配不是更丑了，完全体现不出专业"
- 原因：装饰性元素（光晕/渐变/脉动/glow）盖过内容 → "产品营销"感而非"专业"感

## v5 确立：极简黑白（克制动身）
- 用户在 3 选 1 中选 A：极简黑白（最克制）
- 全部去主色、去装饰：纯黑底 #0A0A0A + 灰阶白字
- 29492→25889 bytes，精简 14%
- 但用户反馈："导航栏太丑了，下边正文显得松散，没有边界"

## v6 微调：模块化边界
- 解决 v5 的"松散没边界"问题（极简黑白的典型陷阱）
- nav 升级：
  - logo 加大字重字号（1.05rem/600）+ letter-spacing -.3px
  - logo 前加 6x14 白色竖条作品牌标识
  - 强底边（--b-2）+ nav links hover 加 1px 下划线 scaleX 动画
- section 卡片化：每个 section（除 hero）用 1px 边 + 10px 圆角 + 内部 padding 40/30/36 + 微妙 surface gradient（rgba 0.012）
- section-title 升级：字号 .76rem/600/letter-spacing 2px + ::before 6x13 白色竖条（视觉锚点）
- 全部 section 统一 max-width 680（与 hero 对齐）
- 25889→26303 bytes
- git 提交 1b99ba0

## 关键教训（值得沉淀）
- "专业感"= 克制 + 模块化。光是"少"不够，还得有"边界"组织
- 极简黑白的陷阱：松散没边界 → 必须给每个 section 1px 边形成"模块块"
- 导航必须有视觉锚点：竖条/方块/加粗 logo，否则"丑"
- 用户对"丑"的反馈很具体（导航/正文/边界），要直接解决具体问题，不要泛泛"再优化"
- 装饰性元素清单（要"专业"必须避免）：glow/gradient/pulse/shadow/box-shadow 高饱和/text-gradient/emoji 装饰

## 风格教训链
- v3 浅色紫调（v 3.0）→ v4 紫蓝深色（装饰过多）→ v5 极简黑白（松散）→ v6 极简+模块化 ✅
- 教训：每次只改一个变量，迭代更稳；不要一次性做太多

## v7 参考 daheiai.com：暖白+橙+衬线（重大调性翻转）
- 用户提供参考站 daheiai.com（人工大黑 AI 站）
- curl 抓 HTML 分析参考站：暖白 #FAF9F6 底 + 橙 #FF6B00 强调 + Noto Serif SC 衬线标题 + 白卡片浮在底 + 居中 680 宽 + 大留白
- 完全推翻 v6 重做 v7（26303→25765 bytes）
- 重大变化：
  - 配色：深色 → 暖白；强调从无 → 橙色 #FF6B00
  - 字体：sans 为主 → h1 2rem 衬线 400；笔记正文也用衬线
  - 卡片：1px 模块块 → 白卡浮在暖白底（柔和对比）
  - hover：padding 微移 → border 变橙 + translateX(4px)（更生动）
  - section 分隔：模块块 → border-top 横线 + uppercase 0.75rem 标签
  - 去掉 JetBrains Mono（参考站只用 Inter + Noto Serif SC，无 mono）
- git 提交 0b3f15b

## 重大调性转变
- v3-v6 都是冷调（紫/蓝/黑白）
- v7 翻转到暖调（白+橙+衬线）
- "专业"不一定冷色，暖白+衬线+单一暖色强调也是工程师气质（参考 daheiai 就是这路线）
- 衬线标题 = 文化感 + 长文阅读友好（适合笔记站定位）

## 风格教训链（最终更新）
- v3 浅紫 → v4 紫蓝深色（装饰多）→ v5 极简黑白（松散）→ v6 极简+模块化（块状）→ v7 暖白+橙+衬线 ✅
- 教训：用户提供参考站是巨大调性转折点；不要预设方向，每次迭代看清整体气质优先于局部优化

## v8 Shopify Polaris 风格：白底+绿品牌+阴影
- 用户要求"用 shopify 的风格"
- Shopify Polaris 核心：白底 + 绿 #008060 + 微妙阴影层次 + 8px 圆角 + 现代 SaaS 卡片
- 完全推翻 v7 重做 v8（25765→26332 bytes）
- 重大变化：
  - 配色：暖白 → 纯白+灰底双层（#f6f6f7 页面 + #fff 卡片）
  - 强调色：橙 #FF6B00 → 绿 #008060（Shopify 经典绿）
  - 字体：去掉 Noto Serif SC（Shopify 不用衬线）
  - 容器：680 单列 → 1180 宽布局 + grid auto-fit 卡片网格
  - 卡片：1px 边 + hover translateX → 1px 边 + 微妙 shadow + hover translateY + shadow 升级
  - tag：边框 tag → 圆角 999px 胶囊 + accent 浅底
  - logo：衬线文字 → 22x22 绿色圆角方块作品牌
  - 代码块：浅灰底 → 黑底
- git 提交 0e0c3d6

## 第六次迭代教训
- v7 暖白+橙+衬线 → v8 白+绿+阴影
- 用户高频试错（一天 6 个版本），每次都否定前一版：浅紫→深色→黑白→模块化→暖白+衬线→Shopify
- 教训：用户对"专业"和"科技感"的视觉定义在不断收敛
- 这次终于是"现代 SaaS"风了（白+绿+阴影+圆角），符合主流产品经理审美
- 经验：当用户频繁改方向时，每个版本之间"差异"要更显著（避免 v5/v6 那种"同一类风格内微调"被否）

## v9 装饰增强（在 v8 基础上加细节，不换方向）
- 用户反馈："各栏目的icon、边框什么的给我补充上啊，让页面更美观"
- 26332→29919 bytes（+3587 字节，装饰元素丰富起来）
- 新增装饰元素清单：
  - section 标题：前加 4×20 绿色圆角竖条（::before）+ 末尾渐变横线
  - nav logo：24×24 绿色渐变方块（带阴影）作品牌标识
  - nav links hover：下划线 scaleX 0→1 动画
  - hero avatar：双 ring（4px 表面 + 5px 灰边），68×68 渐变绿
  - hero 顶部：48×3 居中绿色装饰条
  - 卡片 hover：左侧/顶部 3px 绿色 accent 边（scaleY/scaleX 0→1 动画）
  - skill pill / filter-btn：圆角 999px 胶囊 + hover 变绿
  - 按钮：主色实心 + hover 加深 + ::after 箭头 →（hover 时右移 3px）
  - view-all：::after 箭头 + hover gap 加宽
  - search-wrap：左侧 ⌕ 搜索图标
  - note-detail / about-page：顶部 48×3 绿色装饰条
  - footer：顶部 60×3 居中绿色装饰条
- git 提交 7eb00fe

## 第七次迭代教训
- 这次用户没否定前一版，而是要求"在 v8 基础上加细节"——说明 v8 方向（Shopify SaaS）终于被接受了
- 关键转折：用户从"换风格"到"加细节"，说明整体方向已被认可
- 经验：装饰元素（绿色装饰条、icon、hover accent 边、按钮箭头）能立刻让"基础好的设计"变成"精致的设计"
- 教训：不要一上来就堆装饰，先把基础设计做好（v8 Shopify 解决了），再加细节（v9）

## v10 赛博玻璃风（深色底+玻璃卡片+青色霓虹）
- 用户反馈 v9 "丑爆了，完全没有一点美感"，审美判断彻底没对齐
- 直接问 4 个方向（深色极简 / 浅色精致 / 内容排版 / 赛博玻璃），用户选 **赛博玻璃风**
- 29919→31869 bytes
- 设计语言：
  - 底：#0A0A0F 深色 + 径向青色/紫色氛围光
  - 卡片：rgba(255,255,255,0.04) 玻璃 + backdrop-filter blur(16px) saturate(180%)
  - 强调：#22D3EE 青色 + 0 0 24px glow
  - nav 毛玻璃半透明 + logo 前青色脉冲圆点
  - hero 顶部 48×3 霓虹装饰条，avatar 青→紫渐变
  - 卡片 hover：底部居中青色霓虹横线（scaleX center 展开）
  - 代码块：深色半透明玻璃底
- git 提交 a52dee5

## 第八次迭代教训
- 经过 10 个版本终于找到了正确的方向——用户对"科技感+专业"的视觉定义 = 深色玻璃+青色霓虹
- 之前 9 个版本（浅紫→深紫→黑白→模块→暖白衬线→Shopify 绿→装饰增强）全偏了
- 关键：不应该一直猜，而应该让用户从几个鲜明差异的方向中选择
- 经验：当用户频繁否定时，不要"在方向上微调"，直接给极端不同的选择让他 pick

## v11 参考 blog.aistar.cool（第 9 次风格切换）
- 用户给了新参考站 blog.aistar.cool，已是第三次用户主动给参考（daheiai → Shopify → aistar）
- curl 抓 HTML + 2 个 CSS 文件，分析出完整设计 Token：
  - 底 #f7fbfe / 卡 #fff / 边 rgba(0,0,0,0.13) / 字 #000 / 次 #43494d / 强调 #007acc
  - 圆角 16px / shadow-sm / shadow-md hover / uppercase letter-spacing 0.24em
  - 渐变文字：background-clip + from-cyan-400 to-blue-500
  - font-weight 800（font-black）用于大标题
  - 字体 HarmonyOS Sans → 替换为 Inter
- 31869→27814 bytes（精简 4000+，因去掉大量玻璃动效和装饰）
- 关键特征：
  - 极浅蓝底 + 纯白卡片（跟 v7 的 #FAF9F6 不同，更偏蓝白）
  - 大圆角 16px（之前都是 8-12px）
  - 渐变标题（不只在 hero，整个站更"干净"）
  - uppercase 间距标签（模仿 aistar 的 tracking-[0.24em]）
  - 无 deep shadow / glow / 玻璃效果（更"平面"更"内容优先"）
- git 提交 ede51ec

## 第九次迭代教训（风格困境）
- "科技感+专业"的视觉定义 11 次都还没锁定——用户每次都给新的参考站
- 每次的参考站风格都完全不同（daheiai=暖白橙衬线 / Shopify 风格=浅绿阴影 / aistar=浅蓝白大圆角）
- 问题可能不在设计质量，而在于用户的审美偏好本身就有很大不确定性
- 建议：后续应该根据参考站精确复制（像素级还原），而不是"取其风格精神"

## 第十次迭代（v12，加入真实图标和装饰）
- 用户反馈"图标和图片都显示不出来，参考站设计看不出来变化"
- 根因：之前版本只用首字母 + 渐变占位，没有 SVG 装饰图、favicon、section 图标、卡片封面
- v12 加了：favicon（内联 SVG）/ 3 个背景径向光斑（20s 慢动画）+ 网格 mask / section 标题前加 SVG 图标 / 项目卡片 140px 渐变封面（5 种颜色+ SVG 装饰图）/ 笔记 mini item 88×88 渐变封面 / Hero 头像 conic-gradient 旋转光环 / h2 标题前加渐变竖条
- 关键经验：参考站"视觉丰富感"主要来自装饰图（icon、封面、动效），纯文字 + 边线永远做不出
- 后续：每个项目/笔记在 config.js / frontmatter 加 `icon: book|product|rocket|chart|code|bulb` 字段即可换封面图

## 第十一次迭代：社交按钮优化
- 用户反馈：社交按钮和文字"不对应"
- 根因：config.js 里有 6 个社交项（Email/GitHub/QQ/微信/RSS/小红书/抖音），但 SOCIAL_ICONS 字典里只有 email/twitter/github/zhihu/wechat/rss/jike/xiaoyuzhou 8 个图标——其中 QQ/小红书/抖音 三个**完全没有图标**，所以那三个按钮只显示文字（QQ、小红书、抖音）
- 图标 14px 太小，对应感差
- 修复：
  1. 补全 5 个新平台图标：QQ（企鹅）/ 小红书（XHS）/ 抖音（音符）/ 哔哩哔哩（电视）/ 微博（眼睛）
  2. 重画了 wechat/weibo/zhihu 三个原图（之前是粗略的，路径错误）
  3. 图标 14px → 18px
  4. 按钮 padding 7x14 → 9x16，gap 6 → 8
  5. hover 效果：border 变蓝、字色变蓝、图标放大 1.1、阴影变蓝
- 经验：所有 SOCIAL_ICONS key 都要和 config.js 的 social key 一一对应；新增社交项时必须同时加图标

## 第十二次迭代：title 同步 + 社交弹窗
- 用户反馈：title 变了没提交（实际已 commit，但 `<title>` 硬编码）+ 社交按钮想跳到用户主页
- 修复：
  1. 加 `document.title = C.cfg.title` 同步 config.title 到浏览器标签
  2. 社交按钮去掉 `target=_blank`，改为站内弹窗（避免 X-Frame-Options 限制）
  3. 弹窗内容：平台图标 + 标题 + 在线 QR 码（api.qrserver.com）+ 打开链接 + 关闭按钮
  4. mailto: 链接（Email）仍直接跳邮件客户端，不走弹窗
  5. ESC 键 + 点击背景 关闭弹窗
  6. 背景 backdrop-filter:blur(8px) 毛玻璃
- 设计：弹窗用 cubic-bezier(.34,1.56,.64,1) 弹性缩放进入，质感不错
- 经验：内联 onclick 用 `return openSocial(event,...)` 时，event 是浏览器自动传入的；但直接 JS 调用要传 null

## 第十三次迭代：社交跳转重构（参考 daheiai.com，推翻上一版弹窗）
- 用户："参考下 daheiai.com 的跳转方式和实现方式"。curl 抓其源码：所有社交链接都是最朴素的 `<a target="_blank" rel="noopener">` 直接跳对应主页，**无弹窗、无二维码生成器**。结构=图标+名称在左、→箭头在右（list 行）。微信视频号这种无网页地址的，`<a href="/shipinhao.jpg" target="_blank">` 直接指向本地二维码图片打开。
- 据此把上一版过度设计的"站内弹窗 + api.qrserver.com 在线二维码"整套拆掉：删 openSocial/ensureSocialModal/closeSocialModal/ESC 监听 + modal HTML + 21 行 modal CSS。
- 新实现：`href = v.url||v.qr`；mailto 不加 target（唤起邮件客户端），其余 `target=_blank rel=noopener` 新标签直跳。config.js 微信加 `qr` 字段（放 assets/wechat-qr.png 即可点击新标签展示二维码，同 daheiai 视频号做法）。
- Playwright 验证：Email=mailto 无 target；GitHub/QQ/RSS/小红书/抖音=target=_blank 直跳；微信因 url/qr 均空被过滤。commit af022b4。
- 教训印证：弹窗+在线二维码是过度设计，直接跳转更简练可靠（不依赖第三方 API），符合"简练不复杂"。同一天内需求反复（v12 刚做弹窗，v13 又推翻），说明外链交互别自作主张加弹窗，直跳最稳。
